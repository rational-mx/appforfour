= simple_form_for(@device_inf) do |f|
  = f.error_notification

  .form-inputs
    = f.input :browser
    = f.input :os
    = f.association :post
    = f.association :comment

  .form-actions
    = f.button :submit
